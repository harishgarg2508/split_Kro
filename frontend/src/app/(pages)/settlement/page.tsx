export default function Settlement() {
    return (
        <div>
            <h1>List all settlement for the current user here </h1>
              {/* const userId = useAppSelector(state => state.user.userId);   get user id like this*/}
            
            
        </div>
    );
}